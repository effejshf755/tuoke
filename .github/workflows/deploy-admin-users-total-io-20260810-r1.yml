name: Deploy Admin Users Total IO 20260810 R1

on:
  push:
    branches:
      - codex/deploy-admin-users-total-io-20260810-r1
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      SSHPASS: ${{ secrets.TUOKE_DEPLOY_PASSWORD_RELAY_STABILITY_V2_R1_RETRY_20260809 || secrets.TUOKE_DEPLOY_PASSWORD_RELAY_STABILITY_V2_DIAG_20260809 }}
      DEPLOY_HOST: 80.253.254.177
      RELEASE_ARCHIVE: deployment/admin-users-total-io-20260810-r1.tgz
      RELEASE_SHA256: 6ee10e4c41245e0f080bb838b0ced05dae92fc2a7f0c121c6d181cb9fb9c44cb
      REMOTE_ARCHIVE: /root/admin-users-total-io-20260810-r1.tgz
      REMOTE_RELEASE: /root/admin-users-total-io-20260810-r1

    steps:
      - uses: actions/checkout@v4

      - name: Install SSH client helper
        run: |
          set -Eeuo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends sshpass

      - name: Configure verified production host key
        run: |
          set -Eeuo pipefail
          install -d -m 0700 "$HOME/.ssh"
          cat > "$HOME/.ssh/known_hosts" <<'EOF'
          80.253.254.177 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIJS003ozsuy8Lbn3Km0Nf/ixxJEFTpLV8aiBEIHc/IO5
          EOF
          chmod 0600 "$HOME/.ssh/known_hosts"

      - name: Upload verified release archive
        run: |
          set -Eeuo pipefail
          test -n "$SSHPASS" || { echo "deployment password secret is unavailable" >&2; exit 1; }
          test -s "$RELEASE_ARCHIVE"
          printf '%s  %s\n' "$RELEASE_SHA256" "$RELEASE_ARCHIVE" | sha256sum -c -
          sshpass -e scp \
            -O \
            -o BatchMode=no \
            -o PreferredAuthentications=password \
            -o PubkeyAuthentication=no \
            -o StrictHostKeyChecking=yes \
            "$RELEASE_ARCHIVE" "root@$DEPLOY_HOST:$REMOTE_ARCHIVE"

      - name: Deploy with database backup and automatic rollback
        run: |
          set -Eeuo pipefail
          sshpass -e ssh \
            -o BatchMode=no \
            -o PreferredAuthentications=password \
            -o PubkeyAuthentication=no \
            -o StrictHostKeyChecking=yes \
            "root@$DEPLOY_HOST" 'bash -s' <<'REMOTE'
          set -Eeuo pipefail
          release='/root/admin-users-total-io-20260810-r1'
          archive='/root/admin-users-total-io-20260810-r1.tgz'
          expected_sha='6ee10e4c41245e0f080bb838b0ced05dae92fc2a7f0c121c6d181cb9fb9c44cb'
          test -s "$archive"
          printf '%s  %s\n' "$expected_sha" "$archive" | sha256sum -c -
          rm -rf -- "$release"
          install -d -m 0700 "$release"
          tar -xzf "$archive" -C "$release" --strip-components=1
          chmod 0700 "$release/deploy.sh"
          PS4='+ deploy.sh:${LINENO}: ' bash -x "$release/deploy.sh"
          REMOTE

      - name: Confirm public service health
        run: |
          set -Eeuo pipefail
          curl --fail --silent --show-error --retry 5 --retry-all-errors \
            https://tuokeapi.com/api/ping
